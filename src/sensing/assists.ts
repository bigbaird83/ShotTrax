/**
 * Watch motion and mic-based shot detect are out of scope for P2.
 * Voice club pick is separate (speech → club name → user still confirms).
 */
export const WATCH_ASSIST = false;
export const MIC_SHOT_ASSIST = false;
