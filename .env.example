# Golf Courses API
# Used for the nearby course picker, hole par, and green centroids (P5).
# Leave blank to run without a key — the picker stays disabled.
# Never commit a real key.
EXPO_PUBLIC_GOLF_COURSES_API_KEY=
