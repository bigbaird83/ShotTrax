# iOS App Icon sizes

Rendered from `docs/design/icon/app-icon.html` (owner flag mark, opaque RGB, no alpha) by `docs/design/icon/render-app-icon.cjs`. Expo prebuild still uses `expo.icon` → `../icon.png` and writes the AppIcon set. These files are the same art at the sizes iOS asks for.
